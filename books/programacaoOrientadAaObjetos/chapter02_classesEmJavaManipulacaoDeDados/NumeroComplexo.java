package books.programacaoOrientadAaObjetos.chapter02_classesEmJavaManipulacaoDeDados;

public class NumeroComplexo {
    void inicializaNumero() {

    }
    /*
    Escreva em Java a classe NumeroComplexo que represente um número complexo.
    A classe deverá ter os seguintes métodos:

    • inicializaNúmero, que recebe dois valores como argumentos para inicializar os campos da classe

    • imprimeNúmero, que deve imprimir o número complexo encapsulado usando a notação
    a + bi onde a é a parte real e b a imaginária

    • éIgual, que recebe outra instância da classe NumeroComplexo e retorna true se os valores dos
    campos encapsulados forem iguais aos da instância passada como argumento

    • soma, que recebe outra instância da classe NumeroComplexo e soma este número complexo com o
    encapsulado usando a fórmula (a +bi)+(c +di) = (a +c)+(b +d)i

    • subtrai, que recebe outra instância da classe NumeroComplexo e subtrai o argumento do
    número complexo encapsulado usando a fórmula (a +bi)−(c +di) = (a−c)+(b−d)i

    • multiplica, que recebe outra instância da classe NumeroComplexo e multiplica este
    número complexo com o encapsulado usando a fórmula (a + bi) ∗ (c + di) = (ac − bd)+(ad + bc)i


    • divide, que recebe outra instância da classe NumeroComplexo e divide o número encap-
    sulado pelo passado como argumento usando a fórmula( (a+bi)/(c+di) = ac+bd/c2+d2 +  bc−ad/c2+d2)i

    */
}
